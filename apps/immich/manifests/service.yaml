apiVersion: v1
kind: Service
metadata:
  name: immich-db
spec:
  selector:
    app: immich-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
