apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: paperless-postgres
spec:
  podMetricsEndpoints:
  - port: metrics
  selector:
    matchLabels:
      cnpg.io/cluster: paperless-postgres
      cnpg.io/podRole: instance
