apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rybbit-postgres
spec:
  podMetricsEndpoints:
  - port: metrics
  selector:
    matchLabels:
      cnpg.io/cluster: rybbit-postgres
      cnpg.io/podRole: instance
