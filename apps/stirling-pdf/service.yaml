apiVersion: v1
kind: Service
metadata:
  name: stirling-pdf
spec:
  selector:
    app: stirling-pdf
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
