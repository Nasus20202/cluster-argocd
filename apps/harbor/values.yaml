exposureType: ingress

ingress:
  core:
    enabled: true
    hostname: harbor.nasus.dev
    annotations:
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
    tls: true

externalURL: https://harbor.nasus.dev

persistence:
  persistentVolumeClaim:
    registry:
      size: 10Gi
    jobservice:
      size: 500Mi
    trivy:
      size: 2Gi

postgresql:
  enabled: false

externalDatabase:
  host: postgres-postgresql.postgres
  user: harbor
  coreDatabase: harbor
  existingSecret: harbor-db-secret
  existingSecretPasswordKey: db-password

existingSecret: harbor-admin-secret
existingSecretAdminPasswordKey: admin-password

redis:
  enabled: true
  master:
    persistence:
      enabled: false
    resources:
      requests:
        memory: 25Mi

portal:
  resources:
    requests:
      memory: 50Mi

core:
  resources:
    requests:
      memory: 50Mi

jobservice:
  resources:
    requests:
      memory: 50Mi
  updateStrategy:
    type: Recreate

registry:
  server:
    resources:
      requests:
        memory: 50Mi
  controller:
    resources:
      requests:
        memory: 50Mi
  updateStrategy:
    type: Recreate

trivy:
  enabled: true
  resources:
    requests:
      memory: 100Mi

migration:
  enabled: false # This can be enabled after the initial setup
  resources:
    requests:
      memory: 50Mi

certificateVolume:
  resources:
    requests:
      memory: 50Mi

metrics:
  serviceMonitor:
    enabled: true
