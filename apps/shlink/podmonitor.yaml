apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: shlink-postgres
spec:
  podMetricsEndpoints:
    - port: metrics
  selector:
    matchLabels:
      cnpg.io/cluster: shlink-postgres
      cnpg.io/podRole: instance
