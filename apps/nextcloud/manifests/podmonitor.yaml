apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: nextcloud-postgres-cnpg-cluster
spec:
  podMetricsEndpoints:
    - port: metrics
  selector:
    matchLabels:
      cnpg.io/cluster: nextcloud-postgres
      cnpg.io/podRole: instance
