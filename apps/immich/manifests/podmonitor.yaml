apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: immich-postgres-cnpg-cluster
spec:
  podMetricsEndpoints:
    - port: metrics
  selector:
    matchLabels:
      cnpg.io/cluster: immich-postgres
      cnpg.io/podRole: instance
